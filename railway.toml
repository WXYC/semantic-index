[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m semantic_index.api"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
